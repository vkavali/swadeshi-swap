# Provided automatically by Railway's Postgres plugin
DATABASE_URL=postgres://user:pass@host:5432/db

# Provided automatically by Railway
PORT=3000

# CORS: "*" allows any origin. Use a comma-separated list to restrict.
# Example: ALLOWED_ORIGINS=https://swadeshi.example.com,https://www.swadeshi.example.com
ALLOWED_ORIGINS=*

# Set to "disable" only for local Postgres without TLS
# PGSSLMODE=disable
