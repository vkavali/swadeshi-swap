// Optional backend configuration.
// Set window.SWADESHI_CONFIG.apiBase to a deployed Swadeshi Swap API URL
// (e.g. "https://swadeshi-api.up.railway.app") to enable community sync.
// Or leave empty and configure at runtime via the ⚙ Settings button.
window.SWADESHI_CONFIG = {
  apiBase: ""
};
